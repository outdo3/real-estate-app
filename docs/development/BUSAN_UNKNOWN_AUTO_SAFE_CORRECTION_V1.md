# E-JIP BUILDING LEDGER UNKNOWN AUTO-SAFE CORRECTION V1

`basic_spec_source = UNKNOWN`인 부산 master 중 AUTO_SAFE로 판정된 세대수 57건과 도로명 94건을 보정한다.

- 날짜: 2026-09-20~21 (KST) · 기준 커밋 `7a77c54`
- **승인된 Production UPDATE**: `total_households` **57** · `road_address` **94** — unique master **149**
- **`basic_spec_source` 변경 0**(provenance 미기록) · parking · pph · FAR · BCR · buildingCount · approvalDate · 좌표 · identity · 캐시 · 서울 · sale · cancellation · schema 전부 불변

## 판정

**PASS** — 승인 집합을 Production에서 전수 재생성해 57 / 94를 정확히 재현했고, 계획한 행만 계획한 값으로 바뀌었다.

---

## 1. 승인 범위와 실제 write

| 필드 | 승인 | 실제 |
|---|---|---|
| `total_households` | 57 | **57** |
| `road_address` | 94 | **94** |
| `basic_spec_source` | **0** | **0** |
| 그 밖 모든 필드 | 0 | **0** |

## 2~3. 대상 재생성

부산 UNKNOWN master **724건 전수**를 현재 Production에서 다시 읽고, safe pager로 대장을 다시 조회해 POLICY B / 단일 도로명 판정을 처음부터 다시 만들었다. 감사 artifact는 쓰지 않았다.

**두 차례 STOP됐다 — 둘 다 맞았다.**

| | 1차 | 재조회 후 |
|---|---|---|
| households AUTO | 57 | **57** |
| roadAddress AUTO | **93** | **94** |
| 미해결 조회 실패 | **7** | 3(기지의 미평가) |

1차에서 도로명이 93으로 나온 것은 **못 읽은 7건 중 하나가 도로명 대상이었기 때문**이다. 수치가 하나라도 어긋나면 멈추는 규칙과, 미해결 조회 실패를 허용하지 않는 규칙이 함께 걸려 축소 측정된 채로 쓰는 것을 막았다. 7건을 간격 2초로 재조회하자 94가 복원됐다.

남은 3건(`26260-3648` · `26290-2777` · `26290-3234`)은 **감사에서 이미 "미평가 PARTIAL 3"으로 분류돼 §3 EXCLUSION LOCK에 명시된 필지**이고, 대상 집합에 **포함되지 않았다**. `§20`의 중단 조건은 "승인 대상에 대한 미해결 실패"이므로 진행이 맞다. 다만 가드를 느슨하게 풀지 않기 위해, **기대 aptSeq 목록을 명시적으로 받아 집합이 정확히 일치할 때만** 통과시키고, 그 필지가 대상에 섞이면 무조건 중단하도록 구현했다.

## 4. 교집합 / unique

| 구분 | 건수 |
|---|---|
| households-only | 55 |
| roadAddress-only | 92 |
| 양쪽 | **2** |
| **unique master** | **149** |

감사가 예상한 "최대 128"보다 많다. 그때는 **master 단위 AUTO_SAFE 128**을 셌고 PARTIAL_SAFE 21을 따로 뒀는데, 실제 write는 **필드 단위 AUTO 57 + 94**를 기준으로 하므로 두 분류가 걸친 master까지 포함돼 149가 된다. 필드 건수(57 / 94)는 승인값과 정확히 일치한다. 128 + 21 = 149로 맞아떨어진다.

## 5. 제외 검증

| 대상 | 결과 |
|---|---|
| 미평가 48(NO_LOT_KEY 45 + PARTIAL 3) | **건드림 0** · 전부 여전히 UNKNOWN |
| REVIEW 166 | 건드림 0 |
| NO_LOT_KEY 45 | 조회 자체를 시도하지 않음(지번/법정동 코드 없음) |
| 부산 외 aptSeq · 중복 aptSeq | 0 · 0 |
| source가 UNKNOWN이 아닌 행 | 0 |
| 도로명 덮어쓰기 시도 | **0** — 94건 전부 저장값이 null(60) 또는 빈 문자열(34) |

## 6. 쓰기 전 diff lock

승인 밖 필드(parking · pph · FAR · BCR · buildingCount · approvalDate · **basic_spec_source** · mgmBldrgstPk · 좌표)는 스냅샷만 남기고 UPDATE의 `SET` 절에 넣지 않았다.

대표 기대값 사전 대조 — **2 / 2 일치**:

| 단지 | 기대 | 실측 |
|---|---|---|
| 왕자 `26350-165` | 30 → **390** | 일치 |
| 그린파크 `26350-153` | 40 → **200** | 일치 |

**잘림 흔적 39건** — 저장값이 그 지번 개별 동 한 곳의 세대수와 정확히 일치. 감사 결과와 동일하다.

### 감소 11건 — 명시해 둔다

57건 중 **11건은 세대수가 줄었다**. 지금까지의 보정(42 · 37건)이 전부 증가였던 것과 다르므로 근거를 남긴다. POLICY B는 **표제부의 주거 동만** 합산하므로, 저장값이 상가 호실을 포함했거나 다른 집계에서 온 경우 내려간다.

| 단지 | 변화 | 근거 |
|---|---|---|
| 현대1차 `26230-65` | 1,885 → **1,733** | 운동/의료/근린 동(호수 **152**) 제외 — 1,885 − 152 = 1,733으로 정확히 일치 |
| 덕천주공1 `26320-70` | 1,141 → **1,140** | 관리사무소(호수 1) 제외 |
| 당리신익 `26380-103` | 316 → **300** | 부속건축물 4건 제외 |
| 대성타워 `26320-107` | 865 → **816** | 판매/근린 동 2건 제외 |
| 우신뉴타운 `26410-41` | 560 → **552** | 구매시설 제외 |
| 경동 `26410-80` · 한성기린 `26410-7` · 마마 `26290-9` · 동원 · 도시안락 · 화신 | 소폭 감소 | **0세대 제외 없음** — 저장값이 표제부 주거 합계보다 단순히 크다 |

마지막 6건은 제외한 레코드가 하나도 없는데도 줄었다. 즉 **저장값이 표제부의 주거 합계와 애초에 맞지 않았다**(총괄표제부 집계나 구 import에서 온 값으로 보인다). safe pager가 `totalCount` 일치로 COMPLETE를 확인했으므로 원천 쪽 누락은 아니다. 승인된 POLICY B를 그대로 적용한 결과다.

## 7. 도로명 대상

94건 전부 `road_address`가 **null(60) 또는 빈 문자열(34)**이었다. 값이 있는 행을 덮어쓴 경우는 **0건**이며, optimistic guard가 "여전히 비어 있을 때만" 쓰도록 강제했다.

## 8. 캐시 선점

| 항목 | 값 |
|---|---|
| 대상과 겹치는 `apartments` 캐시 행 | **3** |
| 그중 tier1 게이트 충족 | **0** |
| 세대수 보정을 가리는 행 | **0** |

`HOUSEHOLDS_SOURCE_PRECEDENCE_HARDENING_V1` 배포 이후 세대수는 master가 이긴다. 캐시 write **0**.

## 9. Rollback artifact

쓰기 **전** 생성(`tmp/unknown-apply/rollback-2026-09-20T15-16-49-375Z.json`). 149행의 aptSeq · 단지명 · 동/지번 · old/new 세대수 · old/new 도로명 · 공식 근거 · 보정 사유 + 되돌림 SQL. **실행하지 않았다.**

## 10~12. 쓰기 결과와 사후 검증

| 항목 | 결과 |
|---|---|
| households 변경 | **57 / 57** |
| roadAddress 변경 | **94 / 94** |
| unique master | **149** |
| 예상과 다른 값 | **0** |
| **승인 밖 필드 drift** | **0** |
| **`basic_spec_source` 변경** | **0** |
| optimistic guard 불일치 | **0** |
| 미평가 48 건드림 | **0** |
| master 총계 | 부산 **3,438** 불변 |

단일 트랜잭션. 행마다 `apt_seq` + 기대 old 값이 일치할 때만 UPDATE했다.

## 13. 대표 QA (Production 12건)

| 단지 | 화면 |
|---|---|
| **왕자** | 30 → **390세대** |
| **그린파크** | 40 → **200세대** |
| 삼익그린맨션 | 120 → **315세대** |
| 화신1 | 72 → **216세대** |
| 동일파크 | 51 → **151세대** |
| 성암 | 40 → **130세대** |
| 삼익 | 50 → **100세대** |
| **현대1차** | 1,885 → **1,733세대** |
| **덕천주공1** | 1,141 → **1,140세대** |
| 당리신익 | 316 → **300세대** |
| 경동(26410-80) | 642 → **612세대** |
| 마마 | 195 → **190세대** |

12건 모두 **주차 · 용적률 · 건폐율이 여전히 `None`** — 이 집합은 원래 그 값을 갖고 있지 않았고, 이번에도 건드리지 않았다.

## 14. 기능 QA

| 기능 | 결과 |
|---|---|
| 상세 info · score(왕자) · school | 200 |
| large-complex · rankings · supply · sitemap · transactions | 200 · **응답 바이트 수가 쓰기 전과 완전히 동일**(10,595 / 39,513 / 252,785 / 20,168 / 93) |
| dashboard | 200 · 72,871 → **72,820** |

> dashboard 51바이트 변화는 **이번 write 때문이 아니다.** 이 라우트는 `households`를 전혀 읽지 않고(코드 확인), 매매 총건·취소도 불변이다. 응답에 `2026-09-21`까지의 날짜가 들어 있고 작업 도중 **날짜가 바뀌었다** — 기간 기준 집계 창이 하루 이동한 것이다. 같은 시점 재측정 2회 모두 72,820으로 안정적이다.

large-complex 진입 경계는 1,938세대이고 이번 최대값이 현대1차 1,733이라 목록 구성은 바뀌지 않는다.

## 15. UNKNOWN 데이터 상태 재집계

`basic_spec_source`를 바꾸지 않았으므로 **라벨은 여전히 UNKNOWN 724**다. 이는 실패가 아니다. 데이터 상태는 다음과 같이 개선됐다:

| 항목 | 보정 전 | 보정 후 |
|---|---|---|
| UNKNOWN 총계 | 724 | 724 (라벨 불변) |
| 세대수 보유 | 535 | 535 |
| **도로명 보유** | **553** | **647** (+94) |
| 도로명 없음 | 171 | **77** |
| **틀린 세대수 잔존** | 57 | **0** |
| **AUTO 가능한 도로명 공백 잔존** | 94 | **0** |
| parking 보유 | 7 | 7 |
| parkingPerHousehold 보유 | 0 | 0 |

## 16. 미평가 48 / REVIEW 166 상태

| 집합 | 상태 |
|---|---|
| NO_LOT_KEY 45 | 불변 — 지번/법정동 코드가 없어 조회 자체가 불가 |
| PARTIAL 3 | 불변 — 상류 간헐 오류, 대상에 포함되지 않음 |
| REVIEW 166 | 불변 |

## 17~19. 회귀 / 격리

| 항목 | 값 |
|---|---|
| 부산 master | **3,438** 불변 |
| 서울 master | **6,843** 불변 |
| 서울 좌표 보유 | **6,726** 불변 |
| 부산 UNKNOWN | **724** 불변 |
| 매매 총건 | **865,421** 불변 |
| 취소 | **16,345** 불변 |
| **all-canceled groups** | **273** 불변 |
| **suspect upper bound** | **334** 불변 |
| Seoul sale | **46** 불변 |
| 배포 이후 생성 행 · all-canceled 그룹 건드림 · natural key 중복 | 0 · 0 · 0 |

## 20. No-unapproved-write assertion

| 항목 | 값 |
|---|---|
| 허용된 write | `total_households` 57 · `road_address` 94 |
| `basic_spec_source` / provenance | **0** |
| parking · pph · FAR · BCR · buildingCount · approvalDate · 좌표 | **0** |
| 캐시 · 서울 · sale · cancellation · schema | **0** |
| INSERT / DELETE | **0 / 0** |
| runtime `src/` 변경 | **0** |

## 21. 테스트

```
npx tsx --test "scripts/**/*.test.mjs" "scripts/**/*.test.ts"   590 pass / 0 fail
npx eslint (신규 2파일)                                          exit 0
npx tsc --noEmit    신규 파일 0 에러
```

## 22~23. 커밋 / push

감사 커밋 `7a77c54`(로컬 보류분)과 이번 커밋을 함께 push했다. docs/scripts만 포함.

## 24. Blockers

없음.

## 25. 다음 권고

1. **NO_LOT_KEY 45건이 가장 큰 남은 덩어리다.** 지번이 파싱되지 않거나 법정동 코드가 없어 **대장 조회 자체가 불가능**하다. 세대수·도로명 이전에 **identity 보강**이 선행돼야 하고, 그것은 별도 STEP이다.
2. **provenance 미기록 문제는 그대로다.** 라벨이 UNKNOWN으로 남아 있어, 다음 census에서도 이 724건은 또 밖으로 빠진다. `basic_spec_source`를 기록할지는 별도 승인이 필요하다 — 이번에 고친 149건만이라도 표시해 두면 재발을 막을 수 있다.
3. **PARTIAL 3건**은 상류 간헐 오류이므로 시간을 두고 재조회하면 풀릴 가능성이 높다.
4. 남은 항목: 한보장산 `26350-52` 1건 · 이상치 2건(송도탑스빌 3.02 · 구서쌍용스윗닷홈 2.04) · REVIEW 166 · roadAddress-only REVIEW 19 · 서울 117 좌표 · 28행 false-cancel · 서울 sale apply.
