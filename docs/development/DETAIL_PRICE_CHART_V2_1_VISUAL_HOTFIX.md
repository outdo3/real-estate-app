# Detail Price Chart V2.1 — Mobile Visual Hotfix

- Reduced mobile chart nesting by letting only the chart surface offset the detail-panel padding.
- Reduced Recharts side reservations while preserving both price and volume axes.
- Recharts makes the chart SVG focusable for its accessibility layer. Pointer focus now suppresses only the non-`focus-visible` outline; keyboard focus remains visible.
- Kept legend labels unbroken and compact on mobile.
- Kept timeline controls in a compact mobile row without changing filtering semantics.
