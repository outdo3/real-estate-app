# Detail Price Chart V2.3 — Unit Specific Chart

The price chart now renders only after an exact AreaSelector value is present. That value is the existing canonical exclusive-area state shared by the detail timeline. `전체` no longer connects trades from multiple unit types; it asks the user to select a unit instead. No aggregation, API, Unit Master, or sale/jeonse semantics changed.
