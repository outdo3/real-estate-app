# Detail Price Chart V2.3.3 — Chart-local unit selector

The chart now receives the existing `unitMaster` list and `selectedArea` setter from the detail page. Its compact native selector changes the same single state used by the chart, metrics, and timeline. Labels use Unit Master representative pyeong only when supplied, and always retain exact exclusive-area identity.
